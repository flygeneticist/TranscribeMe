<h2>Notes</h2>
<p>
    View, share, and download your past transcription notes here.
    There are: {{eventCount}} events in the db!
</p>

<div style="width: 70%;">
    <table class="table table-striped table-condensed">
        <tr>
            <th class="col-sm-1">Date</th>
            <th class="col-sm-2">Event Description</th>
            <th class="col-sm-1">Note Link</th>
        </tr>
        {{#if userEvents}}
            {{#each userEvents}}
                <tr>
                    <td>{{startDate}}</td>
                    <td>{{description}}</td>
                    <td><a href="#">{{note}}</a></td>
                </tr>
            {{/each}}
            </table>
        {{else}}
            </table>
            <p>There are no current or upcoming events to display.</p>
        {{/if}}
</div>

